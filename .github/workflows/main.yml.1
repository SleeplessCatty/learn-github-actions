# 工作流的名称
name: Say Hello

# 触发工作流的事件
on:
  # 当有代码推送到 main 分支时触发
  push:
    branches:
      - main

# 工作流要执行的任务
jobs:
  # 定义一个名为 "hello-job" 的任务
  hello-job:
    # 指定任务运行在哪个操作系统上，这里我们用最新的 Ubuntu
    runs-on: ubuntu-latest

    # 任务包含的步骤
    steps:
      # 第一步：打印问候语
      - name: Print a greeting
        # "run" 关键字用来执行命令行脚本
        run: echo "Hello, GitHub Actions! This is my first workflow."
